{
  "name": "elm-pages-example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm run generate && elm-pages develop",
    "serve": "npm run build && http-server ./dist -a localhost -p 3000 -c-1",
    "build": "npm run generate && elm-pages build",
    "generate": "node ./scripts/generator.js"
  },
  "postinstall": "elm-tooling install",
  "dependencies": {
    "elm-pages": "^1.5.4"
  },
  "devDependencies": {
    "elm-format": "^0.8.4",
    "elm-tooling": "^0.5.0",
    "http-server": "^0.12.3",
    "node-fetch": "^2.6.1"
  }
}
